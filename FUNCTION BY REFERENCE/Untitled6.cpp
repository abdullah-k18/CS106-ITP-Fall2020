#include <iostream> //Header File
using namespace std; //Namespace

int divide (int &a, int &b) //Function Decleration, Prototypr, And Definition
{
  int* r,x,y; //Variable Decleration
  r*=x*/y*; //r Variable Will Store Division Of Two Numbers
  return (r*); //Value Returniing To Main
}

int main () //Function For Execution
{
	cin>>a>>b;
  cout << divide (a,b) << '\n'; /*Message To Be Displayed On The Console
                                 + Function Calling*/ 
  cout << divide (a,b) << '\n'; /*Message To Be Displayed On The Console
                                   + Function Calling*/ 
  return 0; //Value Returning
}
